# 필독 — 환경 구성 및 트러블슈팅 (Must Read)

> 본 문서는 BrainCo Revo2 로봇 손을 Ubuntu 22.04 / ROS 2 Humble 환경에서 구동할 때
> 실제로 발생한 문제와 그 해소 절차를 **증상 → 원인 → 해결책** 3단 구조로 정리한 것입니다.
> 실행 절차 자체는 [`README.md`](./README.md) 및 [`사용법.md`](./사용법.md) 를 먼저 참조하십시오.

---

## 0. 문제 발생 시 확인 순서 (Triage Order)

아래 순서를 지키면 대부분의 증상이 3단계 안에서 판별됩니다. **소프트웨어를 먼저 의심하지 마십시오.**

| 순서 | 확인 항목 | 확인 명령 / 방법 |
|:--:|---|---|
| 1 | **24 V 전원 어댑터** 연결 및 LED 점등 | 육안 확인 |
| 2 | USB 장치 노드 존재 | `ls -la /dev/ttyUSB*` |
| 3 | 포트 권한 | `groups \| grep dialout` |
| 4 | 포트 선점 프로세스 유무 | `sudo lsof /dev/ttyUSB0` |
| 5 | 파이썬 의존성 | `python3 -c "import bc_stark_sdk"` |

---

## 1. 손이 전혀 응답하지 않는다

### 증상
프로그램은 정상 기동하고 오류도 없으나 손가락이 움직이지 않는다. 또는 로그에
`Failed to detect device on port` / `No available port found` 가 출력된다.

### 원인
**24 V DC 외부 전원이 연결되지 않은 것이 가장 빈번한 원인입니다.** Revo2 는 USB를 통신 전용으로만
사용하며, 액추에이터 구동 전력은 전량 24 V 어댑터에서 공급받습니다. 따라서 전원이 없으면
**시리얼 포트는 정상적으로 열리지만 장치가 Modbus 요청에 응답하지 않습니다.**
소프트웨어 지표만으로는 전원 결손을 구별할 수 없습니다.

### 해결책
1. 24 V 어댑터가 콘센트와 손 본체 양쪽에 물려 있는지, 어댑터 LED가 켜져 있는지 확인합니다.
2. 전원을 연결한 뒤 USB 케이블을 한 번 뽑았다 다시 꽂습니다.
3. 그래도 응답이 없으면 아래로 장치 인식 자체를 확인합니다.

```bash
ls -la /dev/ttyUSB*            # /dev/ttyUSB0 이 보여야 정상
dmesg | tail -30 | grep -i usb # FTDI 브리지 인식 로그 확인
```

---

## 2. `Permission denied: '/dev/ttyUSB0'`

### 증상
포트를 여는 시점에 권한 거부 예외가 발생하며 즉시 종료된다.

### 원인
`/dev/ttyUSB0` 의 소유 그룹은 `dialout` 이며, 사용자 계정이 이 그룹에 속해 있지 않습니다.
그룹을 추가한 직후에도 **현재 로그인 세션에는 반영되지 않습니다** — 그룹 소속은 로그인 시점에
프로세스 자격증명으로 확정되기 때문입니다.

### 해결책

```bash
# 영구 해결 (최초 1회) — 실행 후 로그아웃/재로그인 또는 재부팅이 필요하다
sudo usermod -aG dialout $USER

# 재로그인 전 즉시 해결 (해당 세션 한정, USB 재연결 시 초기화됨)
sudo chmod a+rw /dev/ttyUSB0

# 현재 세션의 그룹 반영 여부 확인
groups | grep dialout
```

> `chmod` 방식은 USB를 뽑았다 꽂거나 재부팅하면 원래 권한으로 돌아갑니다. 매번 반복하지 않으려면
> 반드시 `usermod` 후 재로그인을 한 번 거치십시오.

---

## 3. 포트가 다른 프로세스에 잡혀 있다

### 증상
직전까지 잘 되던 프로그램이 갑자기 장치를 찾지 못한다. 또는 실행 직후 아무 반응 없이 멈춘다.

### 원인
**경로 A(SDK 직접)와 경로 B(ROS 2 컨트롤러 노드)가 동시에 `/dev/ttyUSB0` 을 열었습니다.**
시리얼 포트는 배타 점유 자원이므로, 두 프로세스가 같은 포트에 프레임을 섞어 쓰면
Modbus CRC 검사가 실패하며 양쪽 모두 통신이 붕괴합니다. 이전 실행이 비정상 종료되어
`stark_node` 가 잔류한 경우에도 동일한 증상이 나타납니다.

### 해결책

```bash
# 1) 포트를 점유한 프로세스 확인
sudo lsof /dev/ttyUSB0

# 2) 잔류 컨트롤러 노드 정리
pkill -f stark_node

# 3) 이후 한 가지 방식만 실행한다 (A 또는 B)
```

> **원칙:** 경로 A와 B는 상호 배타적입니다. ROS 2 방식을 쓰는 동안에는 `hand_keyboard.py` 를
> 실행하지 않고, 반대의 경우도 같습니다.

---

## 4. `No module named 'bc_stark_sdk'`

### 증상
`from bc_stark_sdk import main_mod as libstark` 구문에서 ImportError 가 발생한다.

### 원인
제조사 Python SDK가 설치되지 않았거나, 다른 파이썬 인터프리터(가상환경 등)에 설치되어
현재 실행 중인 인터프리터에서 보이지 않습니다.

### 해결책

```bash
pip3 install bc-stark-sdk==0.9.5 colorlog

# 어느 인터프리터에 설치되었는지 확인
python3 -c "import bc_stark_sdk, sys; print(sys.executable); print(bc_stark_sdk.__file__)"
```

> 버전을 `0.9.5` 로 고정하는 이유는, 본 저장소의 코드가 해당 버전의 API
> (`main_mod`, `set_finger_positions_and_durations`, `FingerUnitMode.Normalized`)를 기준으로
> 검증되었기 때문입니다. 상위 버전에서는 모듈 구조가 변경될 수 있습니다.

---

## 5. `[오류] 공식 SDK 예제 폴더를 찾을 수 없습니다`

### 증상
`hand_keyboard.py` 또는 `hand_demo_play.py` 실행 시 위 메시지가 출력되고 종료된다(종료코드 1).

### 원인
본 저장소에는 **제조사 공식 예제가 포함되어 있지 않습니다**(라이선스 미명시 및 바이너리 포함
때문 — README §6 참조). 스크립트는 `revo2_utils.py` 를 공식 예제 폴더에서 import 하므로,
그 위치를 알아야 합니다. 기본값은 참조 설치 위치인 `~/1/brainco/src/stark-serialport-example` 입니다.

### 해결책

```bash
# 1) 공식 예제 확보
mkdir -p ~/1/brainco/src && cd ~/1/brainco/src
git clone https://github.com/BrainCoTech/stark-serialport-example.git

# 2) 다른 위치에 두었다면 환경변수로 지정 (셸을 새로 열 때마다 필요)
export STARK_SDK_DIR=/원하는/경로/stark-serialport-example

# 3) 매번 입력하지 않으려면 셸 시작 파일에 등록
echo 'export STARK_SDK_DIR=~/1/brainco/src/stark-serialport-example' >> ~/.bashrc
```

---

## 6. ROS 2 경로에서 `ros2_stark_msgs` 임포트 실패

### 증상
`from ros2_stark_msgs.msg import SetMotorMulti` 에서 ModuleNotFoundError 가 발생한다.
또는 `ros2 topic pub` 시 메시지 타입을 찾을 수 없다고 나온다.

### 원인
`ros2_stark_msgs` 는 제조사 예제 워크스페이스(`ros2_stark_ws`)에서 **직접 빌드해야 생성되는
사용자 정의 메시지 패키지**입니다. 배포판 apt 패키지로는 제공되지 않습니다. 또한 빌드했더라도
해당 워크스페이스의 `install/setup.bash` 를 source 하지 않으면 파이썬 경로에 등록되지 않습니다.

### 해결책

```bash
# 새 터미널마다 아래 두 줄을 먼저 실행한다 (순서 중요: ROS 2 → 워크스페이스)
source /opt/ros/humble/setup.bash
source $STARK_SDK_DIR/ros2_stark_ws/install/setup.bash

# 등록 확인
ros2 interface show ros2_stark_msgs/msg/SetMotorMulti
```

빌드 자체가 되어 있지 않다면 공식 예제의 `ros2_stark_ws/README.md` 절차에 따라 `colcon build` 를
먼저 수행하십시오.

---

## 7. ROS 2 방식에서 손이 안 움직인다 (오류는 없다)

### 증상
`keyboard_control.py` 는 정상 실행되고 키 입력에 반응하는 로그도 출력되나 손이 움직이지 않는다.

### 원인
**컨트롤러 노드(`stark_node`)가 기동되지 않았습니다.** 경로 B에서 파이썬 스크립트는
`/set_motor_multi_127` 토픽에 메시지를 발행할 뿐이며, 그 토픽을 구독해 시리얼 프레임으로
변환하는 주체는 컨트롤러 노드입니다. 구독자가 없으면 메시지는 조용히 버려집니다.

### 해결책

```bash
# 터미널 ① — 컨트롤러 노드를 먼저 띄운다. 이 창은 켜 둔 채로 유지한다.
cd $STARK_SDK_DIR/ros2_stark_ws && ./stark_serial_manager.sh launch

# 확인: 노드와 구독자가 실제로 존재하는지 검증한다
ros2 node list                      # /stark_node 가 보여야 정상
ros2 topic info /set_motor_multi_127  # Subscription count 가 1 이상이어야 한다
ros2 topic echo /motor_status       # 손의 현재 위치가 흘러야 정상
```

> 구독자 수(`Subscription count`)를 확인하는 것이 핵심입니다. 발행 측 로그만으로는
> 명령이 하드웨어에 도달했는지 판별할 수 없습니다.

---

## 8. 특정 손가락이 예상과 다르게 움직인다

### 증상
`positions` 배열의 3번 인덱스를 바꿨는데 다른 손가락이 움직인다. 문서마다 배열 순서가 다르게 적혀 있다.

### 원인
Revo2 의 6개 액추에이터 배열 순서는 자료에 따라 **두 가지로 다르게 기술**되어 있습니다.
본 저장소의 코드는 실측으로 확정한 순서를 사용합니다.

| 배열 순서 | 출처 / 적용 범위 |
|---|---|
| `[엄지, 엄지보조, 검지, 중지, 약지, 소지]` | **본 저장소 코드의 기준.** `src/tests/` 실측으로 확정 |
| `[엄지, 검지, 중지, 약지, 소지, 손목]` | 초기 문서화 시점의 표기. 손목 축이 있는 구성에 해당 |

값의 의미는 두 경우 모두 `0` = 완전히 폄, `1000` = 완전히 쥠 입니다.

### 해결책
보유 기기의 실제 매핑을 다음 스크립트로 **직접 확인**하십시오. 3초 간격으로 한 손가락씩
구부리므로 눈으로 인덱스를 확정할 수 있습니다.

```bash
export STARK_SDK_DIR=~/1/brainco/src/stark-serialport-example

python3 src/tests/test_finger_index.py   # SDK의 FingerId 정수값 조회
python3 src/tests/test_finger_array.py   # 실제 구동으로 인덱스 확인 (하드웨어 필요)
```

> `src/tests/test_fingers.py` 는 구버전 SDK API(`from bc_stark_sdk import libstark`)를 사용하므로
> `0.9.5` 에서는 동작하지 않을 수 있습니다. 참고용으로만 보존하였습니다.

---

## 9. WSL2(Windows)에서 장치가 보이지 않는다

### 증상
Windows에는 COM 포트로 잡히는데 WSL2 안에서 `/dev/ttyUSB*` 가 존재하지 않는다.

### 원인
WSL2 는 별도 가상 머신으로 동작하므로 호스트 USB 장치가 자동 전달되지 않습니다.
`usbipd-win` 을 통해 장치를 명시적으로 attach 해야 합니다.

### 해결책
[`docs/WSL2_USB_연결_가이드.md`](./docs/WSL2_USB_연결_가이드.md) 의 절차를 따르고,
보조 스크립트로 `tools/connect_usb_auto.ps1`(Windows PowerShell 관리자 권한),
`tools/wait_for_com3.sh` 를 사용하십시오.

> 상시 운용에는 WSL2 보다 **네이티브 Ubuntu 설치를 권장**합니다. USB 전달 단계가 없어
> 재부팅·재연결 시 attach 절차를 반복하지 않아도 됩니다.

---

## 10. 터미널이 이상해졌다 (입력이 화면에 안 보인다)

### 증상
프로그램이 비정상 종료된 뒤 터미널에 입력한 글자가 표시되지 않거나 엔터가 먹지 않는다.

### 원인
키보드 제어는 터미널을 `tty.setraw()` 로 raw 모드로 전환합니다. 정상 경로에서는 `finally`
블록이 원래 속성을 복원하지만, `SIGKILL` 등으로 강제 종료되면 복원 코드가 실행되지 못합니다.

### 해결책

```bash
reset      # 또는
stty sane
```

---

## 참조 문헌 (Cross-validated References)

1. **BrainCo RevoLimb Hand 공식 문서** — 전원 규격(24 V), Modbus RTU 통신 파라미터, FingerId 정의.
   https://www.brainco-hz.com/docs/revolimb-hand/index.html
2. **BrainCoTech / stark-serialport-example** — `revo2_utils.open_modbus_revo2()` 의 포트 자동 탐색 로직,
   `ros2_stark_ws` 빌드 절차 및 `SetMotorMulti` 메시지 정의.
   https://github.com/BrainCoTech/stark-serialport-example
3. **ROS 2 Humble — Topics 및 사용자 정의 인터페이스** — 구독자 부재 시 메시지가 폐기되는 동작,
   `install/setup.bash` 오버레이 규칙. https://docs.ros.org/en/humble/Tutorials.html
4. **Linux `tty_ioctl(4)` / `termios(3)` man page** — raw 모드 전환과 `TCSADRAIN` 복원 시점 규격.
   https://man7.org/linux/man-pages/man3/termios.3.html
5. **Ubuntu Server Guide — Serial device permissions (`dialout` group)** — 그룹 변경이 재로그인 후
   적용되는 이유. https://help.ubuntu.com/community/Serial
6. **microsoft / usbipd-win** — WSL2 USB 장치 전달(attach/detach) 규격.
   https://github.com/dorssel/usbipd-win
7. **Modbus Application Protocol Specification V1.1b3** — 동일 버스 다중 마스터 시 CRC 실패 원인.
   https://www.modbus.org/specs.php
