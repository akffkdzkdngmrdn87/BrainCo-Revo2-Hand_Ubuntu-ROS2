# 🤖 BrainCo Revo2 로봇손 — 키보드 제어 실행법

> 최초 작성 2026-07-10 · Ubuntu 22.04 기준
> **아래 코드블록을 통째로 복사해 터미널에 붙여넣으면 됩니다.**
>
> 명령은 **이 저장소를 내려받은 폴더 안에서** 실행합니다.
> 제조사 예제 폴더를 기본 위치(`~/1/brainco/src/stark-serialport-example`)와 다른 곳에 두었다면,
> 터미널을 열 때마다 아래 한 줄을 먼저 실행하십시오.
>
> ```bash
> export STARK_SDK_DIR=/내려받은/경로/stark-serialport-example
> ```

---

## ⚡ 실행 전 필수 확인 (1가지)

로봇손은 **24V DC 전원 어댑터**가 반드시 연결되어 있어야 작동합니다.
(USB는 통신만 담당. 전원이 없으면 손이 응답하지 않습니다.)

- [ ] 24V 전원 어댑터가 콘센트 + 손 본체에 연결, 어댑터 LED 켜짐
- [ ] USB 케이블 연결됨

---

## ▶ 1. 키보드 실시간 제어 (메인, 터미널 1개)

```bash
python3 src/hand_keyboard.py
```

실행하면 아래 키로 손을 조작합니다. **엔터 없이 키 하나만 누르면 즉시 움직입니다.**

### ✋ 프리셋 제스처
| 키 | 동작 | 키 | 동작 |
|:--:|------|:--:|------|
| `p` | ✋ 보(펴기) | `u` | 👍 엄지 척 |
| `f` | ✊ 주먹 | `g` | 🔫 총 |
| `v` | ✌️ 브이/가위 | `l` | 🤟 러브(ILY) |
| `o` | 👌 OK | `c` | 💗 하트 |
| `n` | 숫자 3 | | |

### 🖐 손가락 단계 순환 — 누를 때마다 한 단계
| 키 | 동작 | 손가락 매핑 |
|:--:|------|------|
| `1` `2` `3` `4` `5` `6` | 폄 → 반쯤 → 쥠 | 1엄지 2엄지보조 3검지 4중지 5약지 6소지 |
| `!` `@` `#` `$` `%` `^` | 쥠 → 반쯤 → 폄 **(반대)** | Shift+숫자, 같은 손가락 순서 |

### 🎚 손가락 미세조정 — 숫자 아랫줄 (q w e r t y)
| 키 | 동작 | 손가락 매핑 |
|:--:|------|------|
| `q` `w` `e` `r` `t` `y` | 쥐는 방향 (+100) | q엄지 w엄지보조 e검지 r중지 t약지 y소지 |
| `Q` `W` `E` `R` `T` `Y` | 펴는 방향 (−100) **(반대)** | Shift, 같은 손가락 순서 |

> 예) 검지를 조금씩 쥐기 → `e` 여러 번 / 검지를 조금씩 펴기 → `E`(Shift+e) 여러 번

### ⚙ 전체 조절 / 기타
| 키 | 동작 |
|:--:|------|
| `Space` | 전체 펴기 |
| `0` | 전체 주먹 |
| `+` (`=`) | 전체 조금 쥐기 |
| `-` (`_`) | 전체 조금 펴기 |
| `i` | 실제 손가락 위치 읽기 |
| `?` | 도움말 다시 보기 |
| `Esc` 또는 `Ctrl+C` | 종료 |

---

## ▶ 2. 전체 동작 데모 플레이 (손이 알아서 모든 동작 시연)

로봇손의 **모든 동작**을 순서대로 자동 시연합니다.
(프리셋 제스처 → 손가락 웨이브 → 3단계 순환 → 미세조정 → 숫자 세기 → 전체 쥐기 레벨 → 피날레)

```bash
python3 src/hand_demo_play.py
```

무한 반복(전시·시연용, `Ctrl+C`로 종료):

```bash
python3 src/hand_demo_play.py --loop
```

> 참고: 프리셋 제스처만 빠르게 보려면 `python3 src/hand_keyboard.py --demo`

---

## ▶ 3. 가장 간단한 동작 확인 (손 펴기만)

```bash
cd $STARK_SDK_DIR/python/revo2 && python3 open_hand.py
```

---

## 🛠 문제 해결

### "Failed to detect device on port" / 손이 응답 없음
1. **24V 전원 어댑터 연결 확인** (가장 흔한 원인)
2. USB 케이블 뽑았다 다시 꽂기
3. 포트 권한 (필요 시):
   ```bash
   sudo chmod a+rw /dev/ttyUSB0
   ```

### 포트가 다른 프로그램에 잡혀 있을 때
```bash
pkill -f stark_node
```

### 손가락 배열 순서 (참고)
```
[엄지, 엄지보조, 검지, 중지, 약지, 소지]
 0 = 완전히 폄   ~   1000 = 완전히 쥠
```

---

## 📌 참고

- 메인 스크립트(키보드 제어): `src/hand_keyboard.py`
- 데모 플레이(전체 동작 시연): `src/hand_demo_play.py`
- 상세 운영 가이드: `docs/운영가이드.md`
- 문제 해결 전문: `필독.md`
- 공식 문서: https://www.brainco-hz.com/docs/revolimb-hand/index.html
